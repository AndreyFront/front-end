import { createStore } from 'vuex'

import signIn from '@/store/modules/signIn'

export default createStore({
  state: {
  },
  getters: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    signIn
  }
})
